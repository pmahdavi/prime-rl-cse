{
  "mcpServers": {
    "prime-rl-modal": {
      "command": "uv",
      "args": [
        "run",
        "--with",
        "mcp",
        "python",
        "/Users/pxm5426/research/prime-rl/prime-rl-cse/modal/mcp_server.py"
      ],
      "description": "Modal deployment and management tools for Prime-RL training",
      "env": {
        "MODAL_TOKEN_ID": "${MODAL_TOKEN_ID}",
        "MODAL_TOKEN_SECRET": "${MODAL_TOKEN_SECRET}"
      }
    }
  }
}
